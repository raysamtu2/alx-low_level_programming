#include "main.h"
/**
 * _samuelislower - function to check for lowercase character
 * @c:  i know tha at the end of thr day int that will use for the argument of the function
 * Return: 0
 */
int _samuelislower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);
}
